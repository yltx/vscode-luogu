function Host (visibilityType) {
  if (visibilityType > 3 && visibilityType < 6) {
    // console.log(visibilityType > 3 && visibilityType < 6)
    // document.getElementById("host").innerHTML = "${getUserSvg(contest['host']['ccfLevel'])}"
    // document.getElementById("hoststyle").style = "${getUsernameStyle(contest['host']['color'].toLowerCase())}"
    console.log('fuck')
  } else {
    console.log('ok')
  }/**/
}
Host(1)
